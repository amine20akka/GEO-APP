import { FilterLayersPipe } from './filter-layers.pipe';

describe('FilterLayersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLayersPipe();
    expect(pipe).toBeTruthy();
  });
});
